# Shared components

This is a static HTML/CSS/JavaScript site without a component framework or shared UI component directory.

Reusable UI patterns are implemented through CSS classes and HTML partial-like sections in `index.html`.
